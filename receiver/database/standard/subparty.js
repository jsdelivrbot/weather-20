import Channel from './channel.js'

export default class Subparty extends Channel {
	constructor(standard, collectionId, subPartyCollectionId){
		super(standard, collectionId, subPartyCollectionId)
	}
}